#include<stdio.h>

int main () {
  
  int num,i;

  printf("enter the number : ");
  scanf("%d", &num);
 if (num <= 1) {
    printf("entered number is not positive");
 }
 for ( i = 2 ; i < num; i++)
 {
    if (num%i == 0) {
        printf("not prime");
        return 0;
    }
}
    printf("prime");
    return 0 ;
}
