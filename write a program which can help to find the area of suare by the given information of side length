/* write a program which can help to find the area of suare by the given information of side length */

# include <stdio.h>

int main () {
    int length;

    printf("enter the side length of squere : \n ");
    scanf("%d", &length );
    printf("area of square is : %d", length*length );

    return 0;
}
