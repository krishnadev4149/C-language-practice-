// simple use of while loop //

#include <stdio.h>
#include <math.h>

int main () {
    int num;

    printf("enter number of line you want : ");
    scanf("%d", &num);

    int i=1;
    while (i <= num) {
        printf("i want %d ice-cream \n", i);
        i++ ;
    }

    return 0;
}
